%h3
  Players

-for clan in @match.clans
  %h4= clan.name
  %ul
    -@match.participations.by_clan(clan).each do |participation|
      %li
        =participation.player.name
        =participation.champion.name


-for player in @match.clans[1].players
  =player.name
